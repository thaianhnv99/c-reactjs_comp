import BottomBody from './BottomBody';
import BottomHeader from './BottomHeader';
import BottomRoot from './BottomRoot';

export const Bottom = Object.assign(BottomRoot, {
  Body: BottomBody,
  Header: BottomHeader,
});
